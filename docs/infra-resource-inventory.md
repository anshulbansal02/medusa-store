# Infrastructure Resource Inventory

Status: preflight template
Last reviewed: 2026-05-24

Use this file during Phase 0 of `docs/infra-terraform-implementation-plan.md`.

Do not paste secrets, tokens, connection strings, private keys, or customer data here. Record resource names, IDs, regions, URLs, owners, and deletion/import decisions only.

## Summary

| Area | Status | Notes |
| --- | --- | --- |
| Vercel | in progress | Separate QA and production storefront projects are Terraform-managed. Both projects are intentionally not linked to Git; manual GitHub Actions dispatch owns deploys. QA app config is Terraform-managed on the QA project and the latest QA deployment is ready on `qa.example.com`. Production has the storefront domains and browser-safe image hostnames only; paid/runtime production backend config remains deferred. |
| Railway | pending | Replace/delete any app hosting, databases, Redis, variables, domains, and workflows if present. |
| Cloudflare | in progress | Terraform manages `example.com` DNS for apex, `www`, `qa`, `qa-api`, and proxied `qa-admin`, plus R2 media buckets/custom domains for QA and production media. R2 custom-domain ownership and SSL are active. QA Access is Terraform-wired as an opt-in setting pending token permission and allowlist confirmation; WAF/Turnstile remain under review. Web Analytics Terraform wiring is present but disabled until the Cloudflare token has RUM write permission. |
| AWS | in progress | Terraform state bucket created in `ap-southeast-1`; obsolete DynamoDB lock table removed after switching to native S3 lockfiles. Production Lightsail resources were removed after the QA-first sequencing decision. QA Lightsail, QA SSM runtime parameters including R2 runtime config, gated production runtime SSM scaffolding, and GitHub Actions OIDC/QA/prod SSM read IAM are Terraform-managed. Continue inventory for billing alerts. |
| Neon | in progress | QA/prod project shell and QA branch/database/role/endpoint are Terraform-managed. Terraform uses the account-supported history retention limit of `21600` seconds and leaves endpoint suspend interval unset. Production migration remains deferred. |
| Upstash | in progress | QA Redis is Terraform-managed as Upstash Global Redis with Singapore primary region. Production Redis is deferred until production setup. |
| Better Stack | in progress | Terraform manages initial uptime monitors for QA storefront, paused QA admin, QA Medusa health/readiness, and a paused production storefront monitor. QA Vector log shipping is configured from a Better Stack Telemetry source. Alert channel test and error-tracking applications remain pending. |
| GitHub | in progress | QA environment has Lightsail deploy variables/secrets and is restricted to manual `dev` deployments. Production environment is restricted to manual `main` storefront deployments and has a required reviewer gate. Production Medusa AWS deploy role/variables remain gated until production Medusa deploy is approved. `dev` allows direct pushes for active development; `main` remains PR-gated for production releases. Latest manual QA Medusa and storefront workflow dispatches completed successfully. Obsolete Railway QA secret was removed. |
| Razorpay | pending | Inventory test/live keys, webhooks, capture settings. |
| Resend | pending | Inventory domains, sender config, API-key ownership. |

## Decisions

Use one row per external resource.

Classification values:

- `keep`: resource remains manual and intentionally unmanaged for now.
- `import`: resource should be imported into Terraform or another declared management path.
- `replace`: resource will be recreated in the accepted target stack.
- `delete`: resource should be removed after explicit approval where needed.
- `deleted`: resource has already been removed.
- `ignore`: resource is unrelated sandbox/history and does not affect production, QA, DNS, billing, or secrets.

| Provider | Resource | Environment | Region | Current owner | Classification | Reason | Required action | Approval needed |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Vercel | `your-storefront-qa-project` project | qa | global functions `sin1` | Terraform shared | keep/import | Existing project ID `prj_placeholder` was imported into Terraform on 2026-05-24 and renamed for QA. It uses Next.js, Node 24.x, Singapore function region, and no Git repository link so manual GitHub Actions dispatch controls deploys. | Keep under shared Terraform. Deploy QA with `vercel deploy --prod` against the QA project. | no |
| Vercel | `your-storefront-prod-project` project | prod | global functions `sin1` | Terraform shared | keep/import | Production project ID `prj_placeholder` is Terraform-managed. It is ready for domain/config scaffolding without production backend runtime cost. | Keep under shared Terraform. Deploy only from the protected production GitHub environment after merge to `main`. | no |
| Vercel | QA project environment variables | qa | global | Terraform shared | keep/import | `NEXT_PUBLIC_SITE_URL`, `MEDUSA_BACKEND_URL`, `NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY`, and `NEXT_PUBLIC_IMAGE_HOSTNAMES` target the QA project's production environment. `MEDUSA_BACKEND_URL` points to `https://qa-api.example.com`; Vercel API verification confirms these env keys exist. | Keep under shared Terraform; rotate publishable keys by creating/revoking keys in Medusa and updating Terraform. Add Web Analytics token after Cloudflare RUM site creation is enabled. | no |
| Vercel | Production project environment variables | prod | global | Terraform shared | keep/import | `NEXT_PUBLIC_SITE_URL` and `NEXT_PUBLIC_IMAGE_HOSTNAMES` target the production project's production environment. Production backend app config remains deferred until production runtime is intentionally enabled. | Keep browser-safe shared values under Terraform; add production backend values only when production runtime is intentionally enabled. Add Web Analytics token after Cloudflare RUM site creation is enabled. | no |
| Vercel | `qa.example.com` project domain | qa | global | Terraform shared | keep/import | Attached to the QA Vercel storefront project and verified in Vercel. Current deployment is ready and returns HTTP 200. | Keep under shared Terraform and deploy QA manually from `dev`. | no |
| Vercel | `www.example.com` project domain | prod | global | Terraform shared | keep/import | Attached to the production Vercel storefront project as the future production storefront domain. | Keep under shared Terraform; production storefront deployment remains manual and protected. | no |
| Vercel | `example.com` apex redirect | prod | global | Terraform shared | keep/import | Terraform-managed 308 redirect to `www.example.com` on the production Vercel project. The module models redirects separately so the redirect target is created first. | Keep under shared Terraform. | no |
| Railway | Medusa app/services | QA/prod | pending | pending | replace/delete | Railway production path is obsolete. | Confirm no production data or active traffic before deletion. | yes if data/traffic exists |
| Cloudflare | `example.com` zone DNS records | shared | global | Terraform shared | keep/import | Domain was purchased in Cloudflare and is the accepted interim launch domain. Terraform manages apex `A` to Vercel, `www` CNAME to Vercel, `qa` CNAME to Vercel, `qa-api` A to the QA Lightsail static IP, and proxied `qa-admin` A to the QA Lightsail static IP. `qa-admin.example.com` record ID is `cloudflare-record-id`. | Keep under shared Terraform; update the explicit QA/prod domain variables when moving to the final brand domain. | no |
| Cloudflare | `your-qa-media-bucket` R2 bucket and `qa-media.example.com` custom domain | qa | global | Terraform shared | keep/import | Low/no-cost QA media storage and public media hostname. Custom-domain ownership and SSL are active. Terraform-managed QA SSM contains the non-secret Medusa R2 runtime values: `S3_FILE_URL`, `S3_BUCKET`, `S3_ENDPOINT`, and `S3_REGION`. QA R2 credentials are stored in SSM `SecureString` and Medusa upload/read has been smoke tested. | Keep credentials out of Terraform state; rotate through Cloudflare and SSM when needed. | no |
| Cloudflare | `your-prod-media-bucket` R2 bucket and `media.example.com` custom domain | prod | global | Terraform shared | keep/import | Low/no-cost production media storage and public media hostname. Custom-domain ownership and SSL are active. Empty bucket does not create production compute/runtime cost. | Create scoped R2 S3 credentials manually and store them in production SSM before enabling production media storage. | no |
| AWS | `your-private-terraform-state-bucket` S3 bucket | shared | ap-southeast-1 | Terraform bootstrap | keep/import | Created by bootstrap for secret-bearing Terraform remote state. Versioning, AES256 encryption, public access block, and HTTPS-only policy verified. | Keep under bootstrap local state; use as prod/QA remote backend. | no |
| AWS | `ecom-terraform-locks` DynamoDB table | shared | ap-southeast-1 | Terraform bootstrap | deleted | Created by the first bootstrap pass for Terraform state locking, but DynamoDB locking is deprecated for the S3 backend. | Removed by Terraform on 2026-05-24 after switching environment backends to `use_lockfile = true`. | no |
| AWS | `ecom-prod-medusa` Lightsail instance | prod | ap-southeast-1a | Terraform prod | deleted | Production instantiation is deferred until QA is set up and tested. | Removed by Terraform on 2026-05-24. Recreate later by enabling production Lightsail in prod Terraform. | no |
| AWS | `ecom-prod-medusa-static-ip` Lightsail static IP | prod | ap-southeast-1 | Terraform prod | deleted | Production instantiation is deferred until QA is set up and tested. | Removed by Terraform on 2026-05-24. Recreate later by enabling production Lightsail in prod Terraform. | no |
| AWS | `ecom-prod-medusa-key` Lightsail key pair | prod | ap-southeast-1 | Terraform prod | deleted | Production instantiation is deferred until QA is set up and tested. | Removed by Terraform on 2026-05-24. Reuse the same local public key path for production later if still appropriate. | no |
| AWS | `ecom-qa-medusa` Lightsail instance | qa | ap-southeast-1a | Terraform qa | keep/import | QA-first Medusa compute host. Ubuntu 22.04, `small_3_0` 2 GB, automatic snapshots enabled at `20:00` UTC. Bootstrapped with Docker, Caddy, Tailscale, Vector, swap, log rotation, and deployment directories. Tailscale IP is kept in private operator notes. | Continue with SSM/runtime config, DNS, and Medusa deployment setup. | no |
| AWS | `ecom-qa-medusa-static-ip` Lightsail static IP | qa | ap-southeast-1 | Terraform qa | keep/import | Static origin IP for QA Medusa API/admin DNS. Live IPv4 is kept in private operator notes and used by `qa-api.example.com` and `qa-admin.example.com`. | Keep for QA API/Admin until QA compute is intentionally replaced. | no |
| AWS | `ecom-qa-medusa-key` Lightsail key pair | qa | ap-southeast-1 | Terraform qa | keep/import | Public key imported from local `~/.ssh/id_ed25519_ecom_lightsail.pub`; private key stays outside Terraform and Git. | Keep for emergency/browser-assisted access; routine SSH uses Tailscale. | no |
| AWS | `/ecom/qa/medusa/NODE_ENV` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret Medusa runtime config for QA; value is `production`. | Keep under QA Terraform; consumed by deploy env-file generation later. | no |
| AWS | `/ecom/qa/medusa/MEDUSA_WORKER_MODE` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret Medusa runtime config for QA; value is `shared` for the single-host QA service. | Keep under QA Terraform; consumed by deploy env-file generation later. | no |
| AWS | `/ecom/qa/medusa/S3_REGION` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret S3-compatible region value for Cloudflare R2 integration; value is `auto`. | Keep under QA Terraform; consumed by QA deploy env-file generation. | no |
| AWS | `/ecom/qa/medusa/STORE_CORS` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret allowed storefront origins for QA, including `https://qa.example.com` plus local development origin. | Keep under QA Terraform; consumed by QA deploy env-file generation. | no |
| AWS | `/ecom/qa/medusa/ADMIN_CORS` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret allowed admin/API origins for QA, including `https://qa-api.example.com` and `https://qa-admin.example.com` plus local development origin. | Keep under QA Terraform; consumed by QA deploy env-file generation. | no |
| AWS | `/ecom/qa/medusa/AUTH_CORS` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret allowed auth origins for QA, including `https://qa-api.example.com`, `https://qa-admin.example.com`, and `https://qa.example.com` plus local development origin. | Keep under QA Terraform; consumed by QA deploy env-file generation. | no |
| AWS | `/ecom/qa/medusa/MEDUSA_BACKEND_URL` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret external Medusa backend URL; value is `https://qa-api.example.com`. | Keep under QA Terraform; redeploy QA Medusa so Caddy and runtime env consume it. | no |
| AWS | `/ecom/qa/medusa/MEDUSA_ADMIN_URL` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret external Medusa Admin URL; value is `https://qa-admin.example.com`. | Keep under QA Terraform; consumed by QA deploy env-file generation and Caddy host routing. | no |
| AWS | `/ecom/qa/medusa/ADMIN_PATH` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret Medusa Admin UI path; value is `/app`. A trial with Medusa `admin.path = "/"` made API `/health` and `/ready` return Admin HTML, so the safe QA shape is Medusa Admin at `/app` with Caddy redirecting the dedicated admin hostname root to `/app`. | Keep under QA Terraform; redeploy QA Medusa before unpausing the Better Stack admin monitor. | no |
| AWS | `/ecom/qa/medusa/S3_FILE_URL` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret public QA media base URL; value is `https://qa-media.example.com`. | Keep under QA Terraform; consumed by Medusa R2 file module once R2 credentials exist. | no |
| AWS | `/ecom/qa/medusa/S3_BUCKET` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret QA R2 bucket name; value is `your-qa-media-bucket`. | Keep under QA Terraform; consumed by Medusa R2 file module once R2 credentials exist. | no |
| AWS | `/ecom/qa/medusa/S3_ENDPOINT` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Non-secret Cloudflare R2 S3-compatible endpoint URL for the account. | Keep under QA Terraform; consumed by Medusa R2 file module once R2 credentials exist. | no |
| AWS | `/ecom/qa/medusa/S3_ACCESS_KEY_ID` and `/ecom/qa/medusa/S3_SECRET_ACCESS_KEY` SSM parameters | qa | ap-southeast-1 | AWS CLI/operator | keep | Secret R2 S3 credentials are intentionally not Terraform-managed because they would enter Terraform state and Cloudflare shows the secret only once when creating the R2 token. | Keep as SSM `SecureString`; redeploy QA Medusa after rotation. | no |
| AWS | `/ecom/qa/medusa/DATABASE_URL` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Secret Neon Postgres pooler URL for QA Medusa, stored as `SecureString`, with `sslmode=verify-full`. | Keep under QA Terraform and do not print value in logs. | no |
| AWS | `/ecom/qa/medusa/REDIS_URL` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Secret Redis TLS URL for QA Medusa, stored as `SecureString`. | Keep under QA Terraform and do not print value in logs. | no |
| AWS | `/ecom/qa/medusa/JWT_SECRET` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Terraform-generated Medusa JWT signing secret for QA, stored as `SecureString`. | Keep under QA Terraform and protect remote state as secret-bearing. | no |
| AWS | `/ecom/qa/medusa/COOKIE_SECRET` SSM parameter | qa | ap-southeast-1 | Terraform qa | keep/import | Terraform-generated Medusa cookie signing secret for QA, stored as `SecureString`. | Keep under QA Terraform and protect remote state as secret-bearing. | no |
| AWS | `/ecom/prod/medusa/NODE_ENV` SSM parameter | prod | ap-southeast-1 | Terraform prod | keep/import | No-cost production runtime scaffold; Standard String parameter, value is `production`. | Keep under prod Terraform; future production Medusa deploy will consume it later. | no |
| AWS | `/ecom/prod/medusa/MEDUSA_WORKER_MODE` SSM parameter | prod | ap-southeast-1 | Terraform prod | keep/import | No-cost production runtime scaffold; Standard String parameter, value is `shared` until dedicated scaling is introduced. | Keep under prod Terraform; update when production worker topology is finalized. | no |
| AWS | `/ecom/prod/medusa/S3_REGION` SSM parameter | prod | ap-southeast-1 | Terraform prod | keep/import | No-cost production runtime scaffold; Standard String parameter, value is `auto` for Cloudflare R2. | Keep under prod Terraform; production media config will add the remaining R2 values later. | no |
| AWS | `/ecom/prod/medusa/JWT_SECRET` SSM parameter | prod | ap-southeast-1 | Terraform prod | keep/import | No-cost production runtime scaffold; Terraform-generated Medusa JWT signing secret, stored as Standard `SecureString`. | Keep under prod Terraform and protect remote state as secret-bearing. | no |
| AWS | `/ecom/prod/medusa/COOKIE_SECRET` SSM parameter | prod | ap-southeast-1 | Terraform prod | keep/import | No-cost production runtime scaffold; Terraform-generated Medusa cookie signing secret, stored as Standard `SecureString`. | Keep under prod Terraform and protect remote state as secret-bearing. | no |
| AWS | `token.actions.githubusercontent.com` IAM OIDC provider | shared | global | Terraform shared | keep/import | GitHub Actions OIDC provider for short-lived AWS credentials. Client ID is `sts.amazonaws.com`; ARN is `arn:aws:iam::123456789012:oidc-provider/token.actions.githubusercontent.com`. | Use from deploy roles; do not create long-lived AWS keys for GitHub Actions. | no |
| AWS | `ecom-qa-github-actions-deploy` IAM role | qa | global | Terraform shared | keep/import | GitHub Actions QA deploy role. Trust is scoped to `repo:your-github-owner/your-github-repo:environment:qa` and `aud=sts.amazonaws.com`; ARN is `arn:aws:iam::123456789012:role/ecom-qa-github-actions-deploy`. | Store ARN as a GitHub QA environment variable for deploy workflow use. | no |
| AWS | `ecom-qa-github-actions-deploy-ssm-read` IAM policy | qa | ap-southeast-1 | Terraform shared | keep/import | Read-only SSM access for QA deploys. Allows `ssm:GetParameter`, `ssm:GetParameters`, and `ssm:GetParametersByPath` only on `/ecom/qa/medusa` and `/ecom/qa/medusa/*`. | Keep attached only to the QA deploy role. | no |
| AWS | `ecom-prod-github-actions-deploy` IAM role | prod | global | Terraform shared | pending | Production Medusa deploy is deferred, so shared Terraform keeps this role disabled by default through `create_prod_deploy_role = false`. | Enable only when production Medusa compute/runtime and deploy workflow are approved. | no |
| AWS | `ecom-prod-github-actions-deploy-ssm-read` IAM policy | prod | ap-southeast-1 | Terraform shared | pending | Production Medusa deploy is deferred, so the production SSM read policy is not created until the role is enabled. | Create only with the production deploy role; scope it to `/ecom/prod/medusa` and `/ecom/prod/medusa/*`. | no |
| Neon | `ecom-medusa` project | prod/qa | aws-ap-southeast-1 | Terraform qa | keep/import | Accepted database project. Provider project ID is kept in private operator notes; PostgreSQL 17; history retention `21600` seconds; default branch is named `prod` with default database/role shape for later production setup. | Keep under QA Terraform for now; do not run production migrations until production setup is explicitly approved. | no |
| Neon | `qa` branch | qa | aws-ap-southeast-1 | Terraform qa | keep/import | QA database branch. Provider branch ID is kept in private operator notes; database and role are both `medusa_qa`. | Keep separate from production data and use only for QA Medusa. | no |
| Neon | `qa` endpoint | qa | aws-ap-southeast-1 | Terraform qa | keep/import | QA read-write endpoint. Provider endpoint ID and host are kept in private operator notes; SSM uses the pooler host output after enabling pooling. | Keep pooler enabled for runtime connection string. | no |
| Upstash | `ecom-qa-medusa` Redis database | qa | global, primary `ap-southeast-1` | Terraform qa | keep/import | QA Medusa Redis. Provider database ID and endpoint are kept in private operator notes; TLS enabled; budget guardrail `$20`; no read regions. | Keep under QA Terraform; monitor usage after QA deploy. | no |
| Upstash | production Redis | prod | Singapore | pending | pending | Accepted target Redis. | Confirm provider region ID and pricing mode. | no |
| GitHub | `qa` environment variables | qa | global | GitHub CLI/manual | keep/import | `AWS_DEPLOY_ROLE_ARN`, `QA_LIGHTSAIL_TAILSCALE_HOST`, `QA_LIGHTSAIL_SSH_KNOWN_HOSTS`, `VERCEL_ORG_ID`, and `VERCEL_PROJECT_ID` are set for deploy/bootstrap. Stale app config variable `MEDUSA_BACKEND_URL` was removed after moving it to Terraform-managed Vercel env config. | Keep deploy/bootstrap values here; keep app config in Terraform-managed platform resources or SSM as appropriate. | no |
| GitHub | `qa` deployment environment policy | qa | global | GitHub CLI/manual | keep/import | QA environment uses a custom deployment branch policy allowing only branch `dev`. QA deploys are manual workflow dispatch, not automatic on every `dev` push. | Keep QA deploys tied to `dev`; do not broaden to feature branches unless the deployment decision changes. | no |
| GitHub | `production` deployment environment policy | prod | global | GitHub CLI/manual | keep/import | Production environment uses a custom deployment branch policy allowing only branch `main` and has required reviewer `your-github-owner`. `prevent_self_review` is false because this is currently a single-admin personal repo. | Keep production deploys manual, approval-gated, and released through PR merge into `main`. Add a second reviewer and enable self-review prevention when another maintainer is available. | no |
| GitHub | `production` environment variables | prod | global | GitHub CLI/manual | keep/import | Vercel org/project IDs are set for production storefront deploys. Production Medusa `AWS_DEPLOY_ROLE_ARN` remains deferred until the production AWS deploy role and Medusa deploy workflow are enabled. | Keep storefront deploy values here. Add Medusa AWS deploy values only when production Medusa deploy is approved. | no |
| GitHub | `qa` environment secret `RAILWAY_PROJECT_ID` | qa | global | GitHub CLI | deleted | Railway deployment path is obsolete. | Removed on 2026-05-24. | no |
| GitHub | `qa` environment deploy secrets | qa | global | GitHub CLI/manual | keep/import | `QA_LIGHTSAIL_SSH_PRIVATE_KEY`, `TS_OAUTH_CLIENT_ID`, and `TS_AUDIENCE` are set. They support GitHub Actions joining Tailscale with `tag:ci` and deploying over SSH. | Keep app runtime secrets in SSM, not GitHub. | no |
| Tailscale | `tag:ci` ACL policy | qa | global | Tailscale admin | keep/import | Tailnet policy allows GitHub Actions nodes tagged `tag:ci` to reach only nodes tagged `tag:ecom-qa-medusa` on TCP 22 and to SSH only as `ubuntu`. QA Lightsail is tagged `tag:ecom-qa-medusa`. | Keep least-privilege; do not restore wildcard tailnet access for CI. | no |
| Tailscale | GitHub Actions federated identity | qa | global | Tailscale admin | keep/import | OIDC trust credential scoped to repo `your-github-owner/your-github-repo` and GitHub environment `qa`; values are stored as GitHub QA secrets. | Keep scope limited to auth key creation for `tag:ci`. | no |
| Better Stack | `QA storefront` uptime monitor | qa | `as` check region | Terraform shared | keep/import | Monitor ID `monitor-id`; checks `https://qa.example.com` every 300 seconds with email and push alerts. | Keep under shared Terraform; test alert delivery before launch. | no |
| Better Stack | `QA Medusa Admin` uptime monitor | qa | `as` check region | Terraform shared | keep/import | Monitor ID `monitor-id`; checks `https://qa-admin.example.com`, follows the root redirect to `/app`, and alerts by email/push. | Keep under shared Terraform. Revisit once Cloudflare Access is enabled because unauthenticated checks may need an adjusted path or policy. | no |
| Better Stack | `QA Medusa health` uptime monitor | qa | `as` check region | Terraform shared | keep/import | Monitor ID `monitor-id`; checks `https://qa-api.example.com/health` for keyword `OK` every 300 seconds, so Admin fallback HTML cannot satisfy it. | Keep under shared Terraform. | no |
| Better Stack | `QA Medusa readiness` uptime monitor | qa | `as` check region | Terraform shared | keep/import | Monitor ID `monitor-id`; checks `https://qa-api.example.com/ready` for keyword `"ready":true` every 300 seconds, covering Postgres and Redis readiness. | Keep under shared Terraform; tune alert behavior if dependency checks are noisy. | no |
| Better Stack | `Production storefront` uptime monitor | prod | `as` check region | Terraform shared | keep/import | Monitor ID `monitor-id`; checks `https://www.example.com` and is intentionally paused until production storefront cutover. | Unpause during production cutover after the production domain is live. | no |
| Better Stack | `ecom-qa-medusa-logs` Telemetry source | qa | germany | Better Stack API/operator | keep | Source ID `source-id`; platform `ubuntu`; created through the Better Stack Telemetry API because the Uptime Terraform provider does not manage Telemetry sources. The account rejected `singapore` and only allowed `germany` for Telemetry data region. Source token is stored in SSM `/ecom/qa/host/BETTER_STACK_SOURCE_TOKEN`. | Keep token in SSM; use `infra/scripts/configure-betterstack-vector.sh` to refresh Vector config. Verify Live Tail after deploy activity. | no |
| Better Stack | `ecom-qa-storefront-errors` Errors application | qa | germany | Better Stack API/operator | keep | Application ID `application-id`; table name `ecom_qa_storefront_errors`; browser token is configured in the QA Vercel project as `NEXT_PUBLIC_BETTER_STACK_BROWSER_TOKEN`. | Keep API-managed until Terraform provider support is reviewed; rotate by updating the Vercel env value and redeploying the storefront. | no |
| Better Stack | `ecom-qa-medusa-errors` Errors application | qa | germany | Better Stack API/operator | keep | Application ID `application-id`; table name `ecom_qa_medusa_errors`; DSN is stored in SSM `/ecom/qa/medusa/BETTER_STACK_ERRORS_DSN`. | Keep API-managed until Terraform provider support is reviewed; rotate by updating SSM and redeploying Medusa. | no |
| AWS | `/ecom/shared/operator/*` SSM parameters | shared | ap-southeast-1 | AWS CLI/operator | keep | Central source of truth for long-lived operator/provider credentials and setup config, including Cloudflare, Vercel, Neon, Upstash, Tailscale, R2, QA admin credentials, and deferred Cloudflare Access email allowlist. Deploy IAM roles do not need this path. | Keep as operator-only SSM parameters; do not print or commit values. | no |

## Manual Inputs Needed

Collect these outside Git:

- Final brand domain later. Interim domain is `example.com` with explicit QA/prod Terraform variables for `www.example.com`, `example.com`, `qa.example.com`, `qa-api.example.com`, `qa-admin.example.com`, `media.example.com`, and `qa-media.example.com`.
- AWS account/profile, state bucket name, and state region.
- Cloudflare admin email allowlist for future Access setup is stored at `/ecom/shared/operator/cloudflare/access/qa_admin_allowed_emails`.
- Vercel token for Terraform and deployment. Current token resolves to the configured operator account context. QA project ID is `prj_placeholder`; production project ID is `prj_placeholder`.
- Upstash account/API-token availability.
- Better Stack account/API-token availability. Local ignored `.env` contains the Better Stack API token and account metadata needed for the initial Terraform-managed uptime monitors.
- Neon API key for Terraform provider auth is stored in SSM at `/ecom/shared/operator/neon/api_key`; CLI OAuth login is not sufficient for Terraform.
- Tailscale admin/operator identities for SSH and GitHub Actions deploy access.
- Razorpay QA/live keys and webhook setup ownership.
- Resend sender domain and API-key ownership.

## Current Repo Cleanup

- Railway deployment workflow: removed from the repo.
- Medusa `railway.json`: removed from the repo.
- Medusa Railway package scripts: removed from the repo.
